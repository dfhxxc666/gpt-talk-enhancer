# GPT TalkEnhancer v0.5.3 WIP Checkpoint

状态：Archived / v0.5.3 released
日期：2026-09-20
基线提交：f5ab9c4745f8a32a56a491ba536e73cfbd60b68e
runtime：0.5.3
定位：Recovery & Consolidation

## 1. Phase 0 目标

在不改变已实机通过的运行行为前提下，建立 v0.5.3 的维护基线。

本阶段只做：

- 固定稳定性验收矩阵。
- 固定 Desktop v3 regression suite。
- 清理过时版本语义与测试命名。
- 同步当前 main 的测试数与构建 SHA。
- 明确 v0.5.3 release 前必须满足的自动门禁和实机门槛。

本阶段不做：

- runtime 版本 bump。
- Timeline Search。
- Prompt Library v2。
- fast path 自动运行。
- bootstrap.js 大重构。
- Work 行为改变。

## 2. 当前稳定基线

当前 main 已包含并实机通过：

- Normal Chat。
- Pinned Chat。
- Project / 文件夹 Chat。
- Mobile / other-device Chat true-top bootstrap。
- silent visual freeze。
- identity recovery。
- stale Timeline cache repair。
- duplicate / missing order repair。
- Q navigation 与 repair scroll ownership 互斥。
- Work Earlier / Later / physical tail / restore compatibility。

当前用户最近一次实机回归确认：

- 手机 Chat Q16 缺失：已修复。
- Q13 定位上下跳动：已修复。
- Timeline order health：恢复连续。
- 用户反馈：无问题。

## 3. 自动基线

Phase 0 起点：

- npm run check：284 / 284 PASS。
- npm run build：PASS。
- npm run build:market：PASS。
- CHAT_PREDICTIVE_FAST_PATH=ABSENT。
- market SHA-256：576B45744AD682405C42831670906059A5E4EE60E8CC601E0C42E699C26A9627
- Desktop bundle SHA-256：19B003F9A02A61828C6DF4286508BB31AE8DC521F5C66D0CC74134AEA51737CA

新增固定命令：

npm run test:v3-regression

该命令只运行 Desktop v3 的 app / core / host / ui 四组测试，用于日常快速回归；Phase 0 实测为 220 / 220 PASS。最终发布仍必须运行完整 npm run check。

## 4. Phase 0 改动范围

允许：

- package scripts。
- tests 名称与测试组织。
- README / docs。
- 诊断与验收记录。

禁止：

- src/v3 运行逻辑改动。
- Host / Navigation / Cache 行为改动。
- release tag。
- version bump。

## 5. Phase 0 退出条件

- test:v3-regression PASS。
- npm run check PASS。
- build:market PASS。
- node --check PASS。
- git diff --check PASS。
- predictive fast path 仍 ABSENT。
- runtime 仍 0.5.1。
- 无运行时代码 diff。

通过后进入 Phase 1：Cache / order invariant consolidation。

## 6. Phase 0 结果

- test:v3-regression：220 / 220 PASS。
- npm run check：284 / 284 PASS。
- npm run build：PASS。
- npm run build:market：PASS。
- node --check：PASS。
- git diff --check：PASS。
- CHAT_PREDICTIVE_FAST_PATH=ABSENT。
- src/v3 runtime diff：0。
- runtime version：仍为 0.5.1。
- market SHA-256：576B45744AD682405C42831670906059A5E4EE60E8CC601E0C42E699C26A9627。
- Desktop bundle SHA-256：19B003F9A02A61828C6DF4286508BB31AE8DC521F5C66D0CC74134AEA51737CA。

Phase 0 完成，下一步进入 Phase 1：Cache / order invariant consolidation。

## 7. Phase 1-2 结果

- TimelineCache 增加 loadWithHealth 与 order health 诊断。
- Chat cache 对 invalid id/order、duplicate id/order、内部 order gap fail-closed。
- 损坏 cache 仅保留 self-describing trusted anchors；无可信 anchor 时拒绝恢复。
- TurnIndex.replaceDomSnapshot 在清空旧 index 前验证完整 snapshot，失败保持旧状态不变。
- Chat bootstrap、repair、manual Earlier、navigation 的 scroll ownership 增加统一只读诊断。
- Chat bootstrap 运行时 manual Load All 不再启动第二个滚动任务。
- Desktop v3 quick regression：226 / 226 PASS。
- Full suite：284 / 284 PASS。
- predictive fast path：ABSENT。
- runtime：0.5.3。
